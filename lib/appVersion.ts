import pkg from "../package.json";

/**
 * 実行中のアプリの版番号（例: "v1.41.0"）。ビルド時に package.json から確定する。
 *
 * リリース後の不具合対応で「その事象はどの版で起きたか」を即座に判別するため、
 * 障害履歴・通話ログの全件に自動で書き添え、係員画面の隅にも表示する
 * （「リリース後の不具合対応の仕組みと流れ」第4章の提案①）。
 */
export const APP_VERSION = "v" + pkg.version;
