# Changelog

## [1.20.12] - 2026-07-31

### 追加（通話中にお客様が言語を選び直せるようにした）
- お客様チャット画面の操作列に **「言語を変える」ボタン**（現在の言語の国旗つき）を追加。キャンセルボタンの隣に置いた
- 押すと8言語の一覧が出て、選ぶと **確認のポップアップ** を挟んでから切り替わる（押し間違いをそのまま通さないため）
  - 確認は **変更後の言語** で表示し、その下に **今の言語** でも同じ問いを併記する。押し間違えても意味が分かるようにするため
  - 決定ボタンは変更後の言語、取り消しボタンは今の言語で表示する
- 切り替えると、画面の文言（見出し・注釈・キャンセル・マイクON/OFF）、**係員の発話の翻訳先**、**アバターの声の言語** がすべて新しい言語になる
- **マイクON中は変更できない**。認識中の音声を取りこぼすため、ボタンを薄く表示し、押されたら理由（「マイクをOFFにすると言語を変えられます」相当）を各言語で説明する
- 切り替え前の会話はそのまま残す（訳し直すと翻訳をやり直すことになるため）
- 係員画面の言語表示も追従するようにした（サーバーから `session:langChanged` を通知）。サーバー側の受け口 `session:setLang` は以前から存在したが未使用だったため、送信側と係員への通知を追加した

### 検証
- ローカルで実際に通話を確立して確認
  - 日本語 → フランス語：画面全体がフランス語に切替。係員の同じ日本語の発話が、切替前は翻訳なし・音声合成 `[ja]`、切替後は "Merci d'avoir utilisé notre service aujourd'hui." に翻訳され音声合成 `[fr]` になることを確認
  - フランス語 → 日本語：翻訳が止まり、音声合成が `[ja]` に戻ることを確認
  - 係員側が `session:langChanged` を受信することを確認
  - 確認ポップアップが「変更後の言語＋今の言語」の2段で出ること、現在の言語が一覧で選べない状態になることを確認
- **マイクON中のロックは、表示のみ確認**（ブラウザ枠内ではマイクを使えないため、分岐を一時的に強制して描画を確認し、元に戻した）。実際にマイクをONにした状態での動作は実機での確認が必要
- 型チェック・本番ビルド合格。書式チェックは変更前と同数（21件）

## [1.20.11] - 2026-07-31

### 変更（お客様チャット画面のさらなる表示整理）
- マイクボタン内の **キー案内「[スペースキー]」「[Space]」を削除**。ボタンには状態（マイクON／OFF、Mic ON／OFF）だけを表示する
  - キーボードのスペースキーでマイクを切り替える動作自体はそのまま残している（案内表示のみを消した）
- アバターの下の **「お話し中...」表示を削除**。これでアバターの下には何も表示されなくなった
  - 発話中かどうかは従来どおり親画面へ通知しており、動作には影響しない
- 外国語選択時の **「キャンセル」ボタンを英語「Cancel」に統一**（従来は取消／취소／Annuler／Cancelar／ยกเลิก と言語ごとにバラバラだった）。日本語選択時は「キャンセル」のまま

### 検証
- ローカルで実際に通話を確立し、**フランス語と日本語の両方で目視確認**。マイクボタンにキー案内がないこと、アバターの下に何も出ないこと、フランス語で「Cancel」・日本語で「キャンセル」と表示されることを確認
- 型チェック・本番ビルド合格。書式チェックは変更前と同数（既存の3件）

## [1.20.10] - 2026-07-31

### 変更（キオスク画面の表示整理）
- アバターの下の **「お気軽にどうぞ」を削除**（外国語のお客様には読めないため）。アバターが話している間の「お話し中...」は従来どおり表示する
- マイクON時の **「Listening...」を削除**し、マイクの状態を示す表記に変更
- マイクボタンの表示を **状態表示（マイクON／マイクOFF）** に統一
  - 日本語：**「マイクON」「マイクOFF」**（キーの案内は「[スペースキー]」）
  - 外国語（7言語共通）：**「Mic ON」「Mic OFF」**（キーの案内は「[Space]」）
  - 従来の「タップしてマイクをON」は状態表示に置き換えた
- 認識中の途中経過テキストは従来どおりボタン内に表示する

### 検証
- ローカルで実際に通話を確立し、**英語・日本語の両方で目視確認**。マイクOFF時の表記（Mic OFF／マイクOFF）、アバター下の文言が消えたこと、見出し・注釈・キャンセルの言語切替を確認
- 型チェック・本番ビルド合格。書式チェックは変更前と同数（既存の3件）

## [1.20.9] - 2026-07-31

### 変更（キオスク画面の多言語化と注釈）
- お客様が外国語を選んだとき、見出し「ご用件をお伺いします。」とボタン「キャンセル」を**選択した言語で表示**するようにした（8言語）
- 通話中の画面に、**AIが自動処理していることの注釈**を見出しの下に追加
  - 日本語：「AIで自動文字起こしをしています。」
  - 外国語：「AIで自動文字起こしと翻訳をしています。」に相当する文を各言語で表示
  - 各言語の文は日本語へ訳し戻して意味がずれていないことを確認済み
- 文言は `UI_TEXT`（旧 `STATUS_TEXT`）に集約。既読・準備中の表示と同じ場所で管理する

### 変更（係員画面の券面カメラを大きく）
- **1件だけ対応しているときは、券面カメラを縦横2倍で表示**するようにした（224×160 → 448×320）。券面の文字が読み取りやすくなる
- **複数通話で画面が分割されているときは場所が足りないため従来の大きさのまま**（`soloView` で切り替え）

### 検証
- ローカルで実際に通話を確立（疑似係員が自動応答）し、**英語と日本語の両方で画面を目視確認**。見出し・注釈・ボタンがそれぞれの言語で表示されること、係員の発話が翻訳されて届くことを確認
- 型チェック・本番ビルド合格。書式チェックの警告は変更前と同数（既存のもの）

## [1.20.8] - 2026-07-31

### 変更（係員画面に、自分が話した日本語と訳文の両方を表示）
- 係員自身の発話について、**話した日本語**と**お客様に届いた訳文**の両方を表示するようにした
  - これまでは日本語のみ表示していた（訳文はサーバーから届いていたが画面で使っていなかった）
  - 送信時に吹き出しの目印（`clientId`）を添え、訳文が返ってきたらその吹き出しに書き足す方式。**同じ文言を続けて話しても取り違えない**
  - 訳文の見出しを話者によって切り替えるようにした（お客様の発話の訳文＝「日本語」、係員の発話の訳文＝お客様の言語名）
  - 検証：疑似キオスク（英語）と疑似係員で実際の通話（呼び出し→応答→発話）を通し、6項目すべて確認。用語集の訳語固定（馬喰横山 → Bakuroyokoyama）も併せて動作
- なお**お客様の画面は従来どおり、選択した言語のみを表示する**（日本語は出さない）。これは仕様であり不具合ではない

## [1.20.7] - 2026-07-30

### 不具合修正（外国語の用語集が大文字小文字の違いで効かない）
- 用語集の照合を**大文字小文字を区別しない**ようにした
  - 外国語欄に `toneri` と小文字で登録していても、英語の音声認識は固有名詞を `Toneri` と大文字で書き出すため、区別すると訳語の固定がほとんど効かなかった（v1.20.6 の検証で発覚）
  - 実測での改善：`I lost my wallet near Toneri` → 修正前「**トネリ**の近くで財布をなくしました」／修正後「**舎人**の近くで財布をなくしました」。`TONERI`（全部大文字）や `suica`（登録は SUICA）も固定されるようになった
  - **語の一部には当てない**：英数字だけの語は語の区切りでのみ一致させる（`Toneriville` `tokyoite` は置換しない。実測で確認）。日本語などの語は区切りの概念がないためそのまま一致させる（従来どおり）
  - 検証12項目：修正前9件 → 修正後**12件すべて合格**（大文字小文字の4パターン／誤爆2件／後戻り確認6件）

## [1.20.6] - 2026-07-30

### 不具合修正（用語集の訳語が意味不明な言葉に化ける）
- 外国語→日本語の翻訳で、用語集に登録した語が **「グロスオターム」のような意味不明なカタカナ**になることがある問題を修正
  - 原因：訳語を固定するために、翻訳前に登録語を目印の文字列に置き換えているが、その目印 `GLOSS0TERM` を **Google 翻訳が訳してしまう**ことがあった。差し戻しに失敗して目印の訳が画面に残っていた
  - 実測：`I want to change trains at GLOSS0TERM` → **「グロスオタームで乗り換えたい」**（同一文10回中4回）。`GLOSS0TERM` 単体では **「用語集」**と訳された
  - 目印を **`[[0]]`** に変更（`[[1]]` が `[[11]]` の一部に誤って一致しないよう、閉じの `]]` を持つ形）
  - 検証：実際に使う14方向（7言語×双方向）35回で一度も壊れないことを確認。さらに実際の登録語を使った通し確認10件で、旧8件→**新9件**が正しく固定された（残る1件は目印とは無関係の大文字小文字の不一致。下記）
- 参考：候補の比較実測 … `GLOSS0TERM` 10回中4回壊れ／`#0#` 10回中2回壊れ（「0番駅」になる）／`[[0]]` 0回

### 判明した既存の制約（未修正・記録）
- 用語集の照合は**大文字小文字を区別する**（`processed.includes(src)`）。外国語欄に `toneri` と小文字で登録していると、英語の音声認識が固有名詞として `Toneri` と大文字で書き出した場合に**一致せず、訳語が固定されない**（実測で確認）。外国語欄を音声認識が出す表記に合わせるか、照合を大文字小文字を無視する形にするかの判断が必要

## [1.20.5] - 2026-07-30

### 不具合修正（性能検証の自動測定）
- 「発話終了→確定テキスト表示」の測定が、**実態とかけ離れた値になっていた**問題を修正
  - これまでは「最後に音声データが届いた時刻」を発話の終わりとみなしていた。しかし本番設定ではお客様側のマイクは通話中ずっとONのままで、黙っている間も無音の音声データが流れ続けるため、基準時刻が常に直前に更新されてしまっていた
  - **実測での比較**：実際の待ち時間が約1.7秒だった発話で、旧方式の記録値は **0.00〜0.04秒**（＝ほぼ常に0秒と記録される）
  - 無音ゲート（v1.18.3）が測っているチャンク音量を流用し、「**人の声とみなせる音量だった最後の時刻**」を発話終了とするようにした。同じ発話での記録値は **2.00〜2.04秒**（声が消えた瞬間からの時間。音声の末尾には無音があるため、待ち時間の実測1.7秒より約0.3秒長く出る）
  - 音声認識の動作自体には影響しない（測定の基準時刻のみの変更）
- あわせて確認できたこと：マイクをONのままにしていても、話し終えてから**約1.7秒**で確定テキストが届く（無音を検知して区切られる）。長時間待たされることはなく、無音ゲートが正規の認識結果を捨てることもない

### 変更（測定CSV）
- 手入力だった「同時接続数」欄を廃止し、**通話時間の重なりから自動判定する「同時通話数」**に置き換えた（1＝単独、2＝2件同時）。「2件同時対応時の遅延増加」の集計に手入力が不要になる
  - 古いログで終了時刻が無い場合は「開始時刻＋通話秒数」で補う。単体テスト8項目で確認（隣接する通話を同時と誤判定しないこと等）
- 常に空欄だった「テキスト欠落件数」欄を廃止（欠落・文字化けは人の評価に移すため）
- スタッフ画面からダウンロードするCSVと `scripts/measure-report.mjs` の両方を同じ内容に更新

## [1.20.4] - 2026-07-30

### 変更（用語集の登録テンプレート）
- `用語集登録テンプレート.xlsx` の「記入ガイド」を、よみの必須化（v1.20.3）に合わせて更新
  - yomi の必須欄を「任意」→「○」に変更
  - 説明を「認識した文字を登録どおりの表記に直すために使用。英字を含む語（SUICA など）は読み上げにも使われます」に変更
  - 入力例の `yomi:（空欄でOK）` を `yomi: ていきけん` に変更
  - 注意書きを「yomi は必須です。すべての語にひらがなの読みを入れてください。未入力の行は登録されません。」に変更
  - 書式（列幅・見出し）を保つため、xlsx 内のテキストのみを直接書き換えている（列構成 ja / yomi / en / zh / zh-TW / ko / fr / es / th は変更なし）

## [1.20.3] - 2026-07-30

### 不具合修正（用語集を丸ごと読み上げたような表示を防ぐ）
- お客様が駅名をひとつ言っただけなのに、**登録済みの用語が丸ごと並んだ意味不明なテキスト**が表示されることがある問題に対処
  - 実測例：「えっちゅうじままで行きたい」と発話 → `越中島、東京駅、大鳥居、幡ヶ谷、御徒町、神保町、越中島、半蔵門、荏原町、洗足池、喜多見 SUICA 馬喰横山、千駄ヶ谷、京王稲田堤`
  - 原因は認識モデルに渡している用語集のヒントで、モデルがヒントの一覧をそのまま復唱してしまう暴走。同じ音声を用語集なしで認識させると正しく取れる。**登録語を増やすほど起きやすい**（登録27語での測定で50回中1回＝2%）
  - この暴走は「登録語と読点だけが並び、助詞や述語がまったく無い」という特徴があるため、**異なる登録語が4語以上あり、かつ登録語・区切り以外の文字が全体の20%以下**のときに認識結果を破棄するようにした（無音のときの幻聴を捨てる仕組みと同じ方針）。破棄はログ `[stt-dump]` に残る
  - 普通の問い合わせ文は駅名を並べても必ず助詞が入るため誤って捨てない。単体テスト20項目で確認（「御徒町から神保町、半蔵門を通って篠崎まで行けますか」のような4駅名を含む文も残る／登録が3語以下のときは破棄しない）
  - 破棄された場合はお客様の発言が表示されない（意味不明な文字列を出すよりは良い、という判断）。係員は聞き直せる

### 変更（用語集の登録）
- 用語登録で**「よみ」を必須**にした（画面・APIの両方で必須。Excel / CSV の一括登録も、よみが未入力の行は登録せず、どの語が未入力かを画面に表示する）
  - 認識した文字を登録どおりの表記に直す後処理と、英字を含む語の読み上げが「よみ」に依存しており、未入力だと**用語を登録しても表示が直らない**ため
  - 実測（難読駅名20種×音声2通り）でも、正しい表記に直しているのは主に「よみ」を使った後処理（認識モデルの生の出力は55%、後処理を通すと75%）

### 調査結果（用語集ヒントの効果と副作用・記録）
- 同一音声での厳密な比較（難読駅名20種×音声2通り×2条件＝100回、登録27語）
  - 用語集**なし**：モデルの生出力 32% → 表示 **70%**／用語集を丸ごと吐く異常 0回
  - 用語集**あり**：モデルの生出力 55% → 表示 **75%**／同異常 1回（50回中）
  - → **用語集のヒントは有効（+5ポイント）なので維持する**。ただし上記の暴走が伴うためガードを入れた
- **普通の問い合わせ文には一切影響しない**ことを確認（登録語を含まない5文×2音声＝20回で、用語集あり／なしの出力が1文字も違わなかった）。干渉は難読な固有名詞を発話したときに限定される
- ヒントの強度（boost）は 5 / 10 / 20 で結果が完全に同一。**強度調整では副作用は消えない**
- 「別の登録語に化ける」誤り（狸穴町→`馬喰横山町`）は機械的に判別できないため現状は受け入れ。性能検証テストでは「実害が大きい誤り」として件数を記録する

## [1.20.2] - 2026-07-30

### 不具合修正（用語集の駅名が登録どおりに表示されない）
- 「まみあなちょう（狸穴町）」と発話したとき、`まみ穴町` のように**一部だけ漢字**になった表示が登録表記に直らない問題を修正
  - 原因は、文を解析して読みを照合する部分での読みの食い違い。「町」の読みを解析側は**マチ**と付けるのに対し、用語集の登録よみは「まみあなちょ**う**」＝**チョウ** で、末尾だけ一致せず矯正が空振りしていた
  - 末尾が地名によくある漢字（町・谷・田・山・川 など）の語は、**その漢字の別読みに差し替えた読み**も照合候補に含めるようにした（狸穴町なら マミアナチョウ と マミアナマチ の両方で照合）
  - 実測：`まみ穴町` `マミ穴町` → `狸穴町` に矯正。従来直っていた形（全かな／かな＋接尾辞漢字）が引き続き直ること、普通の会話文24種が誤って書き換わらないことも確認（自動テスト39項目すべて合格）。実音声でも既存の矯正（碑文谷・舎人・東京駅）と一般語の素通り（隣）に後戻りがないことを確認
  - あわせて、接尾辞漢字の表が server/sttStream.ts と lib/reading.ts で二重管理になっていたのを lib/reading.ts に一本化

### 調査結果（今回は直していない・記録）
- **音そのものが別の語に聞こえた場合は復元できない**：碑文谷→`伊母屋`、舎人→`隣`／`遁地`。「読みが1文字違いなら直す」案も試作したが、`この電車は東京行きですか` → `この電車は東京駅ですか` のような実害のある誤変換が出たため採用しない
- **用語集の登録語同士が干渉する**：同一音声でのA/B比較で、「まみあなちょう」の音が**用語集ありのとき4回中2回 `馬喰横山町`** になった（用語集なしでは4回すべて `まみあな町`）。認識モデルへのヒント（boost 20）が無関係な登録語を引き寄せている。難読語を多く登録するほど起きやすいと考えられ、要検討事項として記録

## [1.20.1] - 2026-07-29

### 不具合修正（用語集の表記と読み上げ）
- 「スイカ」と発話したとき、表示が「suica」と「SUICA」で揺れる問題を修正
  - 原因は、認識モデルが同じ音をカタカナで出したり英字で出したりするため。カナで出れば読みの置換で登録表記になるが、英字で出た場合は読みの照合対象にならず素通りしていた
  - 用語集の**日本語欄に英字を含む語**について、大文字小文字を区別せず登録どおりの表記に揃えるようにした（対象は現在 SUICA の1語のみ）。単語の区切りでのみ一致させるため、`basic` のような語の内部や `mysuica` のように英字が続く場合は反応しない
  - 副次効果として、翻訳の用語集（大文字小文字を区別して照合）も正しく効くようになる
- アバターの音声が「SUICA」を「エスユーアイシーエー」と綴り読みすることがある問題を修正
  - 原因は、日本語の読み上げに英大文字が来ると「単語」と「頭文字の略語」のどちらで読むか定まらないこと。加えて Chirp3-HD は生成のたびに音声が変わる（同一テキスト8回で4種類の音声を確認）
  - **実測**: 「SUICA」を12回読み上げると音声長が2,688〜9,408バイト（最大3.5倍差）で揺れ、うち10回が綴り読み（「エスユーアイシーエー」＝7,680バイト）相当の長さだった。「すいか」で読ませた10回はすべて短いまま
  - 日本語の読み上げに限り、英字を含む登録語を用語集の**よみ欄**に置き換えてから合成するようにした。画面表示は登録どおり（SUICA）のまま変わらない
  - 外国語の読み上げは「よみ」欄がないため対象外（英語欄を `Suica` のように登録すると略語に見えにくくなる）

## [1.20.0] - 2026-07-29

### 追加（係員画面）
- 「お客様発話中」の案内を追加。お客様が話し始めてから、確定テキストが表示されるまで点灯し続ける
  - 判定はお客様のマイクのON/OFFではなく**実際の声の大きさ**で行う（将来キオスクのマイクを通話中ずっとONにしてもそのまま動く）。無音ゲート（v1.18.3）の音量測定を流用し、発話とみなせる音量が0.2秒続いた時点で点灯
  - 消灯は確定テキストが係員画面に表示された瞬間。言葉の切れ目（息継ぎ）では消えない
  - 騒音の誤検知などで確定テキストが来ない場合に備え、8秒で自動的に消灯する保険つき
  - アバターの発話中は点灯しない。キオスクのマイクは声の主を区別できずアバター自身の声も拾うため、誤点灯を防ぐ（アバター側の発話開始/終了通知を利用し、終了後0.5秒の余韻を置いて再開）。**停止するのは表示の判定のみで、音声認識と無音ゲートの音量測定は継続する**

### 不具合修正（お客様画面）
- 「係員が回答を準備しています」が、係員のマイクOFF時点で消えてしまい、返答が表示されるまで最大約4.2秒（確定テキスト待ち3.0秒＋翻訳・音声合成1.2秒＝実測）画面が無反応になる問題を修正
  - 返答が実際にお客様の画面に出るまで表示を継続するようにした。文字送信の場合も同様（送信による入力欄クリアでは消さない）
  - 係員が何も話さずにマイクを切った場合は確定待ちの3秒で消灯、通信不良時は60秒で消灯（既存の保険）
  - 係員がマイクを入れたまま会話を続ける場合、マイクONの合図は最初の一度しか出ないため、係員側も声の大きさで検知して2回目以降の返答でも案内が出るようにした

### ドキュメント
- 「遠隔接客アプリ_待ち時間の案内表示_機能仕様書.docx」を作成（Dropbox 20_Document/仕様書・設計書）

## [1.19.0] - 2026-07-29

### 追加（お客様の「伝わったか分からない」不安への対応）
- **既読表示**：お客様の発言が係員の画面に届いた時点で、その吹き出しの下に「✓ 係員に伝わりました」を表示（係員の操作は不要）
  - キオスクが `speech:user` に自前のid（`clientId`）を添えて送り、サーバーが係員へ中継した時点で `speech:delivered` を返す。確定した発言のみが対象（認識途中では出さない）
- **準備中表示**：係員がマイクON中／入力欄に文字がある間、「●●● 係員が回答を準備しています」をアニメーション付きで表示。係員の返答が届いた時点で自動的に消える
  - 係員画面が `staff:composing{active}` を送信（マイクON/OFFはボタン・Space・自動OFFの全経路で反映、入力欄は文字の有無で判定）。サーバーは担当係員からの送信のみ中継（なりすまし防止）
  - 解除通知が届かない場合の保険として、お客様側で60秒経過したら自動的に消す
- 上記2つの文言は対応8言語すべてに用意（日・英・中簡体・中繁体・韓・仏・西・タイ）
- 表示の追加のみで、通話・音声認識・音声合成・翻訳の動作は変更なし

## [1.18.3] - 2026-07-29

### 不具合修正（音声認識まわり2件）
- スタッフ画面でスペースキーを連打すると「マイクへのアクセスに失敗しました。」と表示されることがある問題を修正
  - 原因はマイクを「開く」処理と「閉じる」処理の重なり（マイクの二重取得）。取得完了前の多重開始を防止し、連打時は最後に押した状態へ自動で合わせるようにした。重なりによる無害な空振りはエラー表示しない
- 無音ゲートを追加：無音・微小雑音の区間で音声認識モデルが実在しない言葉を創作する「幻聴」（数の読み上げ・あいづち・挨拶や、用語集登録語の誤挿入）を破棄するようにした
  - サーバー側で音量(RMS)を100msごとに測定し、発話といえる音量（しきい値300・0.2秒以上）が観測されていない区間の確定結果・入力中表示を抑止（`server/silenceGate.ts` 追加）
  - 破棄内容と実測音量は `[stt-gate]`／`[stt-diag]` ログに記録（しきい値調整・再発調査用）
  - 実測：室内無音の音量≈84、通常発話≈5,600〜10,800 → しきい値300は両者の間で妥当を確認

## [1.18.2] - 2026-07-28

### 調査用
- 用語集登録語の誤挿入（発話していない「馬喰横山」「suica」が表示された事象）の調査用に、認識の各段階（モデル生出力→かな漢字補正→読み照合）を記録する `[stt-diag]` 診断ログを追加（動作は不変）
  - このログにより原因がモデルの「幻聴」であることが確定し、1.18.3の無音ゲートにつながった

## [1.18.1] - 2026-07-27

### 不具合修正
- 測定CSVダウンロードをテスト分（端末名が `test-` で始まる通話）のみに絞れるように（`/api/measure?test=1`）。スタッフ画面のボタンはテスト分のみを取得する

## [1.18.0] - 2026-07-27

### 追加
- スタッフ画面に「測定CSVをダウンロード」ボタンを追加。技術者なしでワンクリックで自動測定結果のCSV（BOM付き・Excelでそのまま開ける）を取得できるように（`/api/measure`・要ログイン）

## [1.17.0] - 2026-07-22

### 追加（性能検証テスト用の自動測定）
- 通話ごとに主要な遅延をミリ秒で自動記録し、既存の通話ログ（`SessionLog.metrics`）に相乗りさせてGCSへ保存するようにした（新しい保存の仕組みを増やさず、本番でもそのまま動作する）
  - 測定項目：呼び出し→係員への着信配信／発話終了→確定テキスト／係員の発話確定→音声送出／通話中の切断回数
  - `server/metrics.ts` を追加。socketId から通話を特定できるよう socketServer が解決関数を登録する
- `scripts/measure-report.mjs` を追加。通話ログから評価記入シートの「自動測定結果」タブに貼り付けられるCSVを生成する（本番APIからの取得／ローカルの logs ディレクトリの両方に対応）
- 測定値はサーバー内部の時間であり、画面表示までの時間は別途+αとなる点に留意

## [1.16.0] - 2026-07-19

### セキュリティ強化
- Socket.IO通信に本人確認（ログインCookie検証）を追加。未認証の接続はスタッフ操作（`staff:join`／`call:answer`／`call:reject`）が拒否されるように
  - 接続時に `staff-session` Cookie を検証し、以降スタッフのuid・表示名は自己申告のペイロードでなく検証済みセッションの値を使用（なりすまし登録を防止）
  - キオスク（お客様）側の通信は従来どおり匿名で動作
- なりすまし注入を防止：`speech:user`／`screen:frame`／`screen:share`／`session:setLang` に「送信者＝その通話の当事者か」のチェックを追加（sessionIdを知る第三者による会話・カメラ映像・画面の注入を遮断）
- 通話ログの日付フォルダを日本時間(JST)基準に変更（UTC深夜＝JST早朝の通話が前日フォルダに入る問題を解消）
- 翻訳用語集を「長い原語から置換」に変更（「東京」と「東京駅」を両方登録しても正しく訳し分け）

## [1.15.0] - 2026-07-19

### 追加
- スタッフ定型文（よく使う文をボタン1つで送信）
  - スタッフ個人用・サーバー(GCS)保存。アカウントメニュー「定型文設定」で追加/編集/削除（「保存」では閉じず「閉じる」で閉じる・「保存しました」表示）
  - 通話パネル下部の定型文チップをタップで送信。既存の送信経路を使うため翻訳・音声読み上げも自動
  - API `/api/staff/quick-replies/me`（認証必須・空/重複除去・200字/30件上限）

## [1.14.1] - 2026-07-19

### 修正
- 長文（約3分）の音声でアバターの口パクと音声が途中で途切れる不具合を修正
  - 原因：音声認識の区切りごとに届く複数のTTS音声を重ねて再生し、先に終わった音声の終了処理が新しい音声の口パクを止めていた
  - 対策：音声を順番待ちキューに入れて1つずつ再生。口パク・「お話し中」表示は最後の音声が終わるまで継続

## [1.14.0] - 2026-07-19

### 追加
- 接客前のマイクテスト（ライブ音量バー）をスタッフ画面に追加
  - アカウントメニュー「マイクテスト」。話すと緑のバーが動き、マイクが正常に音を拾えているか確認できる（従来の「マイク許可確認」を置き換え）

## [1.13.0] - 2026-07-17

### 追加・変更
- 用語集の登録テンプレート（Excel）に「よみ」列と記入ガイドを追加
- マイクON時の表示を強化：赤い大バナー「🎤 マイクON　あなたの声がお客様に届いています」＋赤枠で一目で分かるように

## [1.12.0] - 2026-07-17

### 追加・修正
- スタッフも連続音声入力に対応（送信後にマイクが自動OFFにならず、話し続けられる）
- スペースキー＝押している間だけマイクON（トランシーバー方式）。マイクボタンは従来どおりクリックでON/OFF、両者は独立
- マイクON/OFFを素早く連打した際の不具合を修正（"Stream timed out"エラー・話し終わりの1〜2文字欠け・スペースキーが効かなくなる問題）

## [1.11.0] - 2026-07-17

### 変更（辞書つき音声認識）
- 音声認識をV2（chirp_2）＋インライン辞書に切り替え、難読駅名などの専門用語を正しく認識（旧V1の用語ヒントは効いていなかった）
- 用語集に「よみ」欄を追加。カナ→漢字置換・読み照合（形態素解析）の後処理で、同音の別漢字（例：用賀↔洋画）まで矯正
- スタッフの表示名を登録アカウント名に固定（自由入力で別名を名乗れる問題を解消）

## [1.10.0] - 2026-07-16

### 追加
- ストリーミング音声認識（Google Cloud STT V1）を実装。`NEXT_PUBLIC_STT_MODE=streaming` で有効化、既定は旧方式(legacy)のままの**休眠デプロイ**（本番の挙動は変わらず）

## [1.9.1] - 2026-07-16

### 修正
- オンラインスタッフに自分が二重表示される不具合を修正
- スタッフ画面のカメラ表示を調整（手元カメラの削除・券面表記・着信プレビューの整理）
- キオスクのロゴを都営地下鉄の公式いちょうマークに差し替え

## [1.9.0] - 2026-07-11

### 追加・変更（キオスク画面と音声の刷新）
- キオスク通話画面を都営地下鉄デザインに刷新。駅員アバターを差し替え、音量連動の口パクを実装
- 音声合成を高品質なChirp3-HD（女性ボイス Achernar）に統一（繁体字のみWavenet）
- 長文の音声が「一文が長すぎる」で無音になる不具合を修正（自動分割して合成・連結／分割位置を単語境界に調整）
- 音声の自動再生をタップ操作で確実に解錠（AudioContextの事前解錠）

## [1.8.1] - 2026-07-10

### 修正
- 本番でスタッフ追加時に `Credential ... Invalid character`（Firebase秘密鍵の読み込み失敗）を修正
  - 原因：Railwayに保存した `FIREBASE_PRIVATE_KEY` の改行/クォートのエスケープがローカルと異なり、鍵が壊れていた
  - 対策：`lib/firebase-admin.ts` の鍵正規化を堅牢化（前後クォート除去・`\n`/`\r\n`エスケープ・**base64**のいずれの形式でも読める）
  - 運用：Railwayの `FIREBASE_PRIVATE_KEY` はエスケープ事故の起きない base64 形式で設定する

## [1.8.0] - 2026-07-10

### 追加・変更（スタッフ権限管理の刷新）
- システム管理者を画面から設定できるように変更
  - スタッフ管理画面に「管理者」トグル（盾アイコン）を追加。Firebase権限フラグ isAdmin を付与/解除
  - ログイン判定を「環境変数 FIREBASE_ADMIN_EMAILS（固定管理者・非常用）または権限フラグ isAdmin」のORに変更
  - 固定管理者は「管理者（固定）」表示のみで画面からは変更不可（締め出し防止の失敗対策）
  - 反映は対象者の再ログイン後（画面に案内あり）
- マネージャー権限を廃止（権限として一切使われておらず、操作を分かりにくくしていたため）
  - スタッフ管理のマネージャートグル・バッジ・作成フォーム・一括登録の列・テンプレート列を削除
  - サーバー側の isManager 処理を削除（旧・常に false になっていた判定バグも解消）
- スタッフ一覧を「登録が新しい順（新→旧）」で表示するよう変更

## [1.7.7] - 2026-07-09

### 修正
- キオスクの2カメラが Edge で正面/手元が入れ替わる問題を修正
  - 原因：`enumerateDevices()` の並び順がブラウザ依存（Chrome と Edge で逆）で、順番で割り当てていたため
  - 対策：ブラウザ間で同一の label でソートしてから割り当て、どのブラウザでも同じ物理カメラが正面/手元になるよう固定
  - 併せて、設置の都合で入れ替えたい場合の `?swapcam=1` URLパラメータを追加
- キオスクの「キャンセル」ボタン（通話終了）が効かない問題を修正（v1.7.1 のリグレッション）
  - 原因：v1.7.1 で追加した call:end のガードが「担当スタッフ以外は終了不可」となっており、利用者側の終了までブロックしていた
  - 対策：セッションの担当スタッフ or 利用者本人なら終了できるよう変更（第三者スタッフによる終了は引き続きブロック）

## [1.7.6] - 2026-07-07

### 変更（スタッフ画面・管理メニューの調整）
- スタッフ画面アカウントメニューの「通話ログ」「用語集管理」を、表示したままクリック不可（薄いグレー）に変更
  - 表示フラグ `CALL_LOGS_ENABLED` / `GLOSSARY_ADMIN_ENABLED`（既定 false）を追加。`true` にすれば通常リンクに戻る
- スタッフ画面の管理者メニューに「駅マスター登録」（/admin/stations）を追加
- スタッフ管理画面のナビゲーションから「用語集」ボタンを削除

## [1.7.5] - 2026-07-07

### 追加・変更（スタッフ画面のUI改善）
- ログイン中のアカウント（メールアドレス）を表示：表示名とは別に、どのアカウントでログイン中かを確認できるように
  - ヘッダーのアカウントメニュー内、および「パスワード変更」パネルに表示
- スタッフ画面ヘッダーを整理（管理者で最大13個並んでいたボタンを2つのメニューに集約）
  - アカウントメニュー（👤）：アカウント情報／マイク許可確認／通話ログ／担当駅設定／パスワード変更／（管理者）スタッフ管理・用語集管理／ログアウト
  - キオスクメニュー：デモ用キオスク画面の起動（券売機1番・2番・精算機1番）
  - 常時表示はステータス・オンライン人数・接続状態のみに簡素化。各メニューはメニュー外クリックで閉じる

## [1.7.4] - 2026-07-07

### 変更（コードレビュー指摘対応・バッチ3-b／挙動は不変のリファクタ）
- Next.js 16 で非推奨になった `middleware` 規約を `proxy` に移行（`middleware.ts` → `proxy.ts`、関数名 `middleware` → `proxy`）。起動時の非推奨警告を解消
- 認証チェックの重複を解消：各 API ルートにコピペされていた `requireAdmin` / `requireAuth` を `lib/session.ts` の `requireAdmin` / `getSessionFromRequest` に一本化（11ファイル）
- GCS＋ローカルファイル＋TTLキャッシュの重複実装（用語集・駅・担当駅の3モジュール）を汎用の `lib/jsonStore.ts`（`createJsonStore`）に集約。`getGCSBucket` も `gcsClient` から再利用
- 未使用のデッドコード（`stations/import` の `normalize` 関数）を削除
- 正味 約180行削減（挙動は不変・認証レスポンス／Fresh読み取りの検証済み）

## [1.7.3] - 2026-07-07

### 修正（コードレビュー指摘対応・バッチ3-a）
- 管理画面での用語集・担当駅の変更が通話サーバーに最大5分間反映されない問題を修正（キャッシュ二重化）
  - 通話サーバー（tsx）とNext APIルートは別モジュールインスタンスで、片方のキャッシュ無効化がもう片方に届かないのが原因
  - `getGlossaryTermsFresh()` / `getAssignmentsFresh()`（キャッシュ非経由の読み取り）を追加し、通話サーバーはこれを使って毎回最新を読むよう変更（A案：即反映優先）
  - Next側（管理画面・STT）は従来どおりキャッシュ維持

## [1.7.2] - 2026-07-07

### 修正（コードレビュー指摘対応・バッチ2）
- 未使用かつ認証なしで公開されていた `/api/translate`・`/api/tts` を削除（有料Google APIの外部悪用を防止。実処理は socketServer 内にあり参照ゼロを確認）
- 繁体字（zh-TW）ユーザーにエラー画面が日本語で表示される問題を修正（ERRメッセージ表に台湾繁体字を追加）
- 担当駅ルーティングの抜け穴を修正
  - 判定を `isStaffEligible` / `isStaffAssignedToStation` に一本化
  - `staff:join` のキュー再送を担当駅でフィルタし、再接続・再読込時に担当外の駅の着信が届かないよう修正
  - `call:answer` に担当駅ガードを追加、クライアントは担当駅の読込後にのみ `staff:join` するよう変更（空登録によるフェイルオープンを防止）
- Excel一括登録で数値セルがあると取り込み全体が失敗する問題を修正（`raw: false` で全セルを文字列化・用語集/スタッフ/駅マスターの3画面共通）

## [1.7.1] - 2026-07-07

### 修正（コードレビュー指摘対応）
- 通話ログAPI（`/api/logs`・`/api/logs/[sessionId]`）に認証を追加。未ログインでは401を返すよう修正（従来は誰でも全通話記録を取得可能だった）
  - `lib/session.ts` に共通の `getSessionFromRequest` ヘルパーを追加
- 再接続時に前の通話状態（会話・音声・マイク・セッションID）を確実にクリアするよう修正。共用キオスクで前のお客様の会話が次のお客様に残らないよう `resetCallState` に統一
- 応答レースで負けたスタッフ側に幽霊通話パネルが残る問題を修正。加えてサーバー側に「セッション担当スタッフ以外は発話・終了できない」ガードを追加

## [1.7.0] - 2026-07-06

### 追加
- キオスク側カメラを正面（顔）・手元の2台構成に対応
  - `hooks/useScreenCapture.ts` が特定のカメラ機器（`deviceId`）を指定して撮影できるように拡張
  - `app/user/UserScreen.tsx` で接続カメラを自動検出し、1台目＝正面／2台目＝手元として2系統同時に撮影・配信
  - スタッフ画面（`ActiveCallPanel`）に正面・手元を上下2段で表示（手元をより大きく表示）
- 着信中（応答前）の顔カメラプレビューに対応
  - `call:requested` イベントを追加し、呼び出しと同時にセッションIDをユーザー側へ通知（従来は応答後まで不明だった）
  - 応答前の顔カメラ映像を担当駅の対象スタッフ全員へ中継し、着信カードの横にサムネイル表示
  - 「応答」時に着信中のプレビュー映像をそのまま通話画面へ引き継ぎ（手元カメラは応答後に起動）

## [1.6.0] - 2026-07-06

### 追加
- 対応言語を7言語から8言語に変更：中国語を「簡体」「繁体」の2言語に分割
  - `lib/languages.ts` に `zh-TW`（繁体）を追加、`zh` は簡体として継続
  - 用語集管理画面・登録テンプレート（Excel/CSV）に繁体字用の列を追加

### 修正
- 用語集の言語コード不整合バグを修正：中国語だけ用語集の置換（専門用語の翻訳）が効いていなかった問題を解消
  - `translateWithGlossary` がGoogle翻訳API用に変換済みのコード（`zh-CN`）で用語集を検索していたため、用語集側のキー（`zh`）と一致せず素通りしていた
  - 素の言語コードで用語集を検索し、Google翻訳API呼び出し直前にのみコード変換するよう修正
- 「繁体」の表記ゆれ（「繁体」と「繁體」の混在）を「繁体」に統一

---

## [1.5.0] - 2026-07-05

### 修正
- 担当駅フィルタリングのバグ修正：スタッフの担当駅外から着信が届いてしまう問題を解消
  - ソケット接続時に担当駅情報をサーバーへ送信するよう修正
  - `staff:updateStations` イベントを追加し、担当駅変更を即時反映
  - `Promise.all` + `initialDataLoaded` フラグで非同期フェッチ完了後に送信する実装に変更
- スタッフ・用語集・駅マスター一括登録テンプレートのサンプル行を削除（記入ガイドシートへ移動）
- スタッフ一括登録のエラー件数表示を修正（スキップ件数とエラー件数を分離・Firebase エラー詳細を表示）
- 駅マスター一括登録の列名検出を改善（0件問題を解消）
- 駅コードを必須項目に変更

---

## [1.4.0] - 2026-06 | コミット: c70977f

### 追加
- スタッフ切断検知（「係員との接続が切れました」通知）

---

## [1.3.0] - 2026-06 | コミット: ff7619c

### 追加
- エラーメッセージ機能（U1〜U3・S1・S4・S5）

---

## [1.2.0] - 2026-06 | コミット: 05694c4

### 追加
- STT用語集・翻訳用語集（Excel/CSVインポート対応）

---

## [1.1.0] - 2026-05 | コミット: 4305177

### 追加
- スタッフ認証機能（Firebase Auth + JWT）

---

## [0.9.0] - 2026-05 | コミット: 5f2ef40 ← 現在の本番

### 追加
- Edge対応（Google STT Push-to-Talk）
- GCSログ保存

---

## [0.8.0] - 2026-05 | コミット: c8091b5

### 追加
- Edge対応初期実装（Chrome/Edge自動切替）

---

## [0.7.0] - 2026-04 | コミット: b98ce74

### 追加
- Railway公開デプロイ
- Socket.IO接続先自動判別

---

## [0.6.0] - 2026-04 | コミット: 2344f88

### 変更
- キオスク通話画面UIリニューアル

---

## [0.5.0] - 2026-04 | コミット: e59a3dd

### 追加
- マルチオペレーションシステム（複数同時対応）

---

## [0.4.0] - 2026-04 | コミット: ce9a701

### 追加
- 通話ログ保存・/logs閲覧画面

---

## [0.3.0] - 2026-03 | コミット: 23d8fe0

### 追加
- スペースキーでマイクON/OFF

---

## [0.2.0] - 2026-03 | コミット: 8d2b492

### 追加
- マイク自動OFF・ユーザーカメラ・アバター

---

## [0.1.0] - 2026-03 | コミット: 8d2b492

### 初回リリース
- POC初期リリース
